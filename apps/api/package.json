{
  "name": "@cashcount/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@cashcount/config": "workspace:*",
    "@cashcount/db": "workspace:*",
    "zod": "4.4.3"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/config.test.ts src/pluggy-webhook-schema.test.ts src/webhook-auth.test.ts src/webhook-route.test.ts",
    "test:integration": "vitest run src/webhook-route.integration.test.ts"
  }
}
